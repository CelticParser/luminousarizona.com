---
layout: default
body_classes: page-project
---

<main class="section">
  <div class="container">
    <section class="row mb-6">
      <section class="col">
        {% include 'framework/project-breadcrumb' %}
        <div class="gallery">
          <figure class="gallery-item">
            <a href="/assets/images/{{ image }}">
              <div class="gallery-image">
                {% include 'framework/shortcodes/responsive-image'
                  , src: image
                  , alt: title
                  , ImagePosition: ImagePosition
                %}
              </div>
            </a>
            <figcaption class="gallery-caption" style="float:right">
              &copy;{{ year }} | {{ title }}
            </figcaption>
          </figure>
        </div>
      </section>
    </section>

    <header class="project-heading">
      <h1>{{ title }}</h1>
      {% if subTitle %}
        <!-- TODO Link subTitle to Tripod Section -->
        <p style="font-size: 1.2em">{{ subTitle }}</p>
      {% endif %}
    </header>

    <article class="content" style="width: 100%">
      {% if variantContentHtml != '' %}
        {{ variantContentHtml }}
      {% else %}
        {{ content }}
      {% endif %}
    </article>

    {% include 'framework/image-navigation' %}
    <br>
    <section>
      <h2>Tripod Location for {{ title }}</h2>
      {% include 'theme/tripod-location' %}
      {% include 'theme/tripod-map' %}
    </section>
  </div>
</main>